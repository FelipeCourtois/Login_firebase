import 'package:flutter/material.dart';

class CadastroCamera extends StatelessWidget {
  const CadastroCamera({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}